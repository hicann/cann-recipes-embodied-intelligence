diff --git a/src/lerobot/policies/pi0/modeling_pi0.py b/src/lerobot/policies/pi0/modeling_pi0.py
index 9b6f38a..62e6739 100644
--- a/src/lerobot/policies/pi0/modeling_pi0.py
+++ b/src/lerobot/policies/pi0/modeling_pi0.py
@@ -17,6 +17,7 @@
 import builtins
 import logging
 import math
+import os
 from collections import deque
 from pathlib import Path
 from typing import TYPE_CHECKING, Literal, TypedDict
@@ -52,6 +53,11 @@ from lerobot.utils.constants import (
     OPENPI_ATTENTION_MASK_VALUE,
 )
 
+try:
+    import torch_npu
+except ImportError:
+    torch_npu = None
+
 
 class ActionSelectKwargs(TypedDict, total=False):
     inference_delay: int | None
@@ -59,6 +65,62 @@ class ActionSelectKwargs(TypedDict, total=False):
     execution_horizon: int | None
 
 
+def _use_npu_fusion_attention() -> bool:
+    return (
+        os.environ.get("LEROBOT_PI0_USE_NPU_FUSION_ATTENTION", "0").strip().lower() in {"1", "true", "yes", "on"}
+        or os.environ.get("LEROBOT_PI0_USE_NPU_FUSED_ATTENTION", "0").strip().lower()
+        in {"1", "true", "yes", "on"}
+    )
+
+
+def _repeat_kv_heads(hidden_states: torch.Tensor, num_attention_heads: int) -> torch.Tensor:
+    if hidden_states.shape[1] == num_attention_heads:
+        return hidden_states
+
+    if num_attention_heads % hidden_states.shape[1] != 0:
+        raise ValueError(
+            f"Cannot repeat KV heads: num_attention_heads={num_attention_heads}, kv_heads={hidden_states.shape[1]}"
+        )
+
+    repeat_factor = num_attention_heads // hidden_states.shape[1]
+    batch_size, kv_heads, seq_len, head_dim = hidden_states.shape
+    expanded = hidden_states[:, :, None, :, :].expand(batch_size, kv_heads, repeat_factor, seq_len, head_dim)
+    return expanded.reshape(batch_size, num_attention_heads, seq_len, head_dim)
+
+
+def _npu_fusion_attention_forward(
+    query_states: torch.Tensor,
+    key_states: torch.Tensor,
+    value_states: torch.Tensor,
+    attention_mask: torch.Tensor | None,
+    scaling: float,
+) -> torch.Tensor:
+    if torch_npu is None:
+        raise RuntimeError("torch_npu is required for PI0 fused attention")
+
+    key_states = _repeat_kv_heads(key_states, query_states.shape[1])
+    value_states = _repeat_kv_heads(value_states, query_states.shape[1])
+
+    mask_bool = None
+    if attention_mask is not None:
+        causal_mask = attention_mask[:, :, :, : key_states.shape[-2]]
+        mask_bool = causal_mask < 0
+
+    out = torch_npu.npu_fusion_attention(
+        query_states,
+        key_states,
+        value_states,
+        head_num=query_states.shape[1],
+        input_layout="BNSD",
+        atten_mask=mask_bool,
+        scale=scaling,
+        keep_prob=1.0,
+        sparse_mode=1,
+    )
+    attn_output = out[0] if isinstance(out, tuple) else out
+    return attn_output.transpose(1, 2).contiguous()
+
+
 def get_safe_dtype(target_dtype, device_type):
     """Get a safe dtype for the given device type."""
     if device_type == "mps" and target_dtype == torch.float64:
@@ -92,7 +154,42 @@ def create_sinusoidal_pos_embedding(  # see openpi `create_sinusoidal_pos_embedd
     return torch.cat([torch.sin(sin_input), torch.cos(sin_input)], dim=1)
 
 
+def _compute_qkv_projections(attn: nn.Module, hidden_states: torch.Tensor, fuse_qkv: bool = False):
+    if not fuse_qkv:
+        return (
+            attn.q_proj(hidden_states),
+            attn.k_proj(hidden_states),
+            attn.v_proj(hidden_states),
+        )
+
+    fused_bias = None
+    q_bias = getattr(attn.q_proj, "bias", None)
+    k_bias = getattr(attn.k_proj, "bias", None)
+    v_bias = getattr(attn.v_proj, "bias", None)
+    if q_bias is not None or k_bias is not None or v_bias is not None:
+        if q_bias is None or k_bias is None or v_bias is None:
+            raise ValueError("Expected q/k/v biases to be all present or all absent when qkv fusion is enabled")
+        fused_bias = torch.cat([q_bias, k_bias, v_bias], dim=0)
+
+    fused_qkv = F.linear(
+        hidden_states,
+        torch.cat([attn.q_proj.weight, attn.k_proj.weight, attn.v_proj.weight], dim=0),
+        fused_bias,
+    )
+    split_sizes = [attn.q_proj.out_features, attn.k_proj.out_features, attn.v_proj.out_features]
+    return fused_qkv.split(split_sizes, dim=-1)
+
+
 def sample_beta(alpha, beta, bsize, device):  # see openpi `sample_beta` (exact copy)
+    if (
+        os.environ.get("LEROBOT_PI0_USE_FAST_BETA_SAMPLE", "1") == "1"
+        and float(alpha) == 1.5
+        and float(beta) == 1.0
+    ):
+        device_obj = torch.device(device)
+        if device_obj.type == "npu":
+            return torch.rand((bsize,), dtype=torch.float32, device=device_obj).pow(1.0 / float(alpha))
+
     alpha_t = torch.as_tensor(alpha, dtype=torch.float32, device=device)
     beta_t = torch.as_tensor(beta, dtype=torch.float32, device=device)
     dist = torch.distributions.Beta(alpha_t, beta_t)
@@ -125,10 +222,15 @@ def make_att_2d_masks(pad_masks, att_masks):  # see openpi `make_att_2d_masks` (
     if pad_masks.ndim != 2:
         raise ValueError(pad_masks.ndim)
 
-    cumsum = torch.cumsum(att_masks, dim=1)
-    att_2d_masks = cumsum[:, None, :] <= cumsum[:, :, None]
-    pad_2d_masks = pad_masks[:, None, :] * pad_masks[:, :, None]
-    return att_2d_masks & pad_2d_masks
+    pad_masks = pad_masks.to(torch.bool)
+    att_masks = att_masks.to(torch.int32)
+    cumsum = torch.cumsum(att_masks, dim=1, dtype=torch.int32)
+    att_2d_masks = (cumsum[:, None, :] <= cumsum[:, :, None]).to(torch.bool)
+    pad_2d_masks = torch.logical_and(
+        pad_masks[:, None, :].to(torch.bool),
+        pad_masks[:, :, None].to(torch.bool),
+    )
+    return torch.logical_and(att_2d_masks, pad_2d_masks)
 
 
 def pad_vector(vector, new_dim):
@@ -231,9 +333,14 @@ def compute_layer_complete(
         gates.append(gate)
         input_shape = hidden_states.shape[:-1]
         hidden_shape = (*input_shape, -1, layer.self_attn.head_dim)
-        query_state = layer.self_attn.q_proj(hidden_states).view(hidden_shape).transpose(1, 2)
-        key_state = layer.self_attn.k_proj(hidden_states).view(hidden_shape).transpose(1, 2)
-        value_state = layer.self_attn.v_proj(hidden_states).view(hidden_shape).transpose(1, 2)
+        query_proj, key_proj, value_proj = _compute_qkv_projections(
+            layer.self_attn,
+            hidden_states,
+            fuse_qkv=(i == 0 and getattr(paligemma, "fuse_qkv_projections", False)),
+        )
+        query_state = query_proj.view(hidden_shape).transpose(1, 2)
+        key_state = key_proj.view(hidden_shape).transpose(1, 2)
+        value_state = value_proj.view(hidden_shape).transpose(1, 2)
         query_states.append(query_state)
         key_states.append(key_state)
         value_states.append(value_state)
@@ -254,15 +361,25 @@ def compute_layer_complete(
     )
     batch_size = query_states.shape[0]
     scaling = paligemma.language_model.layers[layer_idx].self_attn.scaling
-    # Attention computation
-    att_output, _ = modeling_gemma.eager_attention_forward(
-        paligemma.language_model.layers[layer_idx].self_attn,
-        query_states,
-        key_states,
-        value_states,
-        attention_mask,
-        scaling,
+    use_npu_fusion_attention = (
+        getattr(paligemma, "use_npu_fusion_attention", False)
+        and torch_npu is not None
+        and query_states.device.type == "npu"
     )
+
+    if use_npu_fusion_attention:
+        att_output = _npu_fusion_attention_forward(
+            query_states, key_states, value_states, attention_mask, scaling
+        )
+    else:
+        att_output, _ = modeling_gemma.eager_attention_forward(
+            paligemma.language_model.layers[layer_idx].self_attn,
+            query_states,
+            key_states,
+            value_states,
+            attention_mask,
+            scaling,
+        )
     # Get head_dim from the current layer, not from the model
     head_dim = paligemma.language_model.layers[layer_idx].self_attn.head_dim
     att_output = att_output.reshape(batch_size, -1, 1 * 8 * head_dim)
@@ -381,15 +498,43 @@ class PaliGemmaWithExpertModel(
 
         self.to_bfloat16_for_selected_params(precision)
 
-    def to_bfloat16_for_selected_params(self, precision: Literal["bfloat16", "float32"] = "bfloat16"):
-        if precision == "bfloat16":
-            self.to(dtype=torch.bfloat16)
-        elif precision == "float32":
-            self.to(dtype=torch.float32)
-            return
-        else:
+    def to_bfloat16_for_selected_params(
+        self, precision: Literal["bfloat16", "float16", "float32"] = "bfloat16"
+    ):
+        precision = precision.lower()
+        if precision not in ("bfloat16", "float16", "float32"):
             raise ValueError(f"Invalid precision: {precision}")
 
+        def try_cast(dtype):
+            try:
+                self.to(dtype=dtype)
+                return True
+            except Exception:
+                return False
+
+        if precision == "float32":
+            target_dtype = torch.float32
+        elif precision == "float16":
+            target_dtype = torch.float16
+        else:
+            if try_cast(torch.bfloat16):
+                target_dtype = torch.bfloat16
+            else:
+                logging.info(
+                    "[WARN] requested bfloat16 but runtime failed to cast; "
+                    "falling back to float16. If you need true bfloat16, "
+                    "use a runtime that supports BF16 kernels."
+                )
+                target_dtype = torch.float16
+                self.to(dtype=torch.float32)
+                if not try_cast(torch.float16):
+                    raise RuntimeError(
+                        "Failed to cast model to float16 on this runtime."
+                    )
+
+        if target_dtype is not torch.bfloat16:
+            self.to(dtype=target_dtype)
+
         params_to_keep_float32 = [
             "vision_tower.vision_model.embeddings.patch_embedding.weight",
             "vision_tower.vision_model.embeddings.patch_embedding.bias",
@@ -535,6 +680,14 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
 
         # Initialize gradient checkpointing flag
         self.gradient_checkpointing_enabled = False
+        self.fuse_paligemma_qkv = (
+            os.getenv("LEROBOT_PI0_FUSE_PALIGEMMA_QKV", os.getenv("PI05_FUSE_PALIGEMMA_QKV", ""))
+            .strip()
+            .lower()
+            in {"1", "true", "yes", "on"}
+        )
+        self.paligemma_with_expert.paligemma.fuse_qkv_projections = self.fuse_paligemma_qkv
+        self.paligemma_with_expert.paligemma.use_npu_fusion_attention = _use_npu_fusion_attention()
 
         # Compile model if requested
         if config.compile_model:
@@ -637,7 +790,7 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
 
         embs = torch.cat(embs, dim=1)
         pad_masks = torch.cat(pad_masks, dim=1)
-        att_masks = torch.tensor(att_masks, dtype=torch.bool, device=pad_masks.device)
+        att_masks = torch.tensor(att_masks, dtype=torch.int32, device=pad_masks.device)
 
         bsize = pad_masks.shape[0]
         att_masks = att_masks[None, :].expand(bsize, len(att_masks))
@@ -702,7 +855,7 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
 
         embs = torch.cat(embs, dim=1)
         pad_masks = torch.cat(pad_masks, dim=1)
-        att_masks = torch.tensor(att_masks, dtype=embs.dtype, device=embs.device)
+        att_masks = torch.tensor(att_masks, dtype=torch.int32, device=embs.device)
         att_masks = att_masks[None, :].expand(bsize, len(att_masks))
 
         return embs, pad_masks, att_masks, adarms_cond
@@ -737,7 +890,7 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         att_masks = torch.cat([prefix_att_masks, suffix_att_masks], dim=1)
 
         att_2d_masks = make_att_2d_masks(pad_masks, att_masks)
-        position_ids = torch.cumsum(pad_masks, dim=1) - 1
+        position_ids = torch.cumsum(pad_masks.to(torch.int32), dim=1, dtype=torch.int32) - 1
 
         att_2d_masks_4d = self._prepare_attention_masks_4d(att_2d_masks)
 
@@ -752,9 +905,16 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
             )
             return suffix_out
 
-        suffix_out = self._apply_checkpoint(
-            forward_func, prefix_embs, suffix_embs, att_2d_masks_4d, position_ids, adarms_cond
-        )
+        if (
+            os.environ.get("LEROBOT_PI0_DISABLE_OUTER_SUFFIX_CHECKPOINT", "0") == "1"
+            and self.gradient_checkpointing_enabled
+            and self.training
+        ):
+            suffix_out = forward_func(prefix_embs, suffix_embs, att_2d_masks_4d, position_ids, adarms_cond)
+        else:
+            suffix_out = self._apply_checkpoint(
+                forward_func, prefix_embs, suffix_embs, att_2d_masks_4d, position_ids, adarms_cond
+            )
 
         suffix_out = suffix_out[:, -self.config.chunk_size :]
         suffix_out = suffix_out.to(dtype=torch.float32)
@@ -798,7 +958,7 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
             images, img_masks, lang_tokens, lang_masks
         )
         prefix_att_2d_masks = make_att_2d_masks(prefix_pad_masks, prefix_att_masks)
-        prefix_position_ids = torch.cumsum(prefix_pad_masks, dim=1) - 1
+        prefix_position_ids = torch.cumsum(prefix_pad_masks.to(torch.int32), dim=1, dtype=torch.int32) - 1
 
         prefix_att_2d_masks_4d = self._prepare_attention_masks_4d(prefix_att_2d_masks)
         self.paligemma_with_expert.paligemma.language_model.config._attn_implementation = "eager"  # noqa: SLF001
@@ -877,7 +1037,7 @@ class PI0Pytorch(nn.Module):  # see openpi `PI0Pytorch`
         full_att_2d_masks = torch.cat([prefix_pad_2d_masks, suffix_att_2d_masks], dim=2)
 
         prefix_offsets = torch.sum(prefix_pad_masks, dim=-1)[:, None]
-        position_ids = prefix_offsets + torch.cumsum(suffix_pad_masks, dim=1) - 1
+        position_ids = prefix_offsets + torch.cumsum(suffix_pad_masks.to(torch.int32), dim=1, dtype=torch.int32) - 1
 
         full_att_2d_masks_4d = self._prepare_attention_masks_4d(full_att_2d_masks)
         self.paligemma_with_expert.gemma_expert.model.config._attn_implementation = "eager"  # noqa: SLF001
diff --git a/src/lerobot/policies/pi0/processor_pi0.py b/src/lerobot/policies/pi0/processor_pi0.py
index 50f5dec..6a917f7 100644
--- a/src/lerobot/policies/pi0/processor_pi0.py
+++ b/src/lerobot/policies/pi0/processor_pi0.py
@@ -14,6 +14,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+import os
 from typing import Any
 
 import torch
@@ -132,7 +133,7 @@ def make_pi0_pre_post_processors(
         AddBatchDimensionProcessorStep(),
         Pi0NewLineProcessor(),  # Add newlines before tokenization for PaliGemma
         TokenizerProcessorStep(
-            tokenizer_name="google/paligemma-3b-pt-224",
+            tokenizer_name=os.getenv("LEROBOT_PI0_TOKENIZER_PATH", "google/paligemma-3b-pt-224"),
             max_length=config.tokenizer_max_length,
             padding_side="right",
             padding="max_length",
